
const Title = ({children}) => {
    return (
        <div className="text-black text-3xl font-bold flex justify-center text-center items-center ">
            {children}
        </div>
    );
};

export default Title;